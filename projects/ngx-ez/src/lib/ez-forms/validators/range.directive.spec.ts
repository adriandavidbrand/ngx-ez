import { RangeDirective } from './range.directive';

describe('RangeDirective', () => {
  it('should create an instance', () => {
    const directive = new RangeDirective();
    expect(directive).toBeTruthy();
  });
});
