import { AlphaStartDirective } from './alpha-start.directive';

describe('AlphaStartDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaStartDirective();
    expect(directive).toBeTruthy();
  });
});
