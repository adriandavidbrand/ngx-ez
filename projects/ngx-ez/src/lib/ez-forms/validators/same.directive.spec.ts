import { SameDirective } from './same.directive';

describe('SameDirective', () => {
  it('should create an instance', () => {
    const directive = new SameDirective();
    expect(directive).toBeTruthy();
  });
});
