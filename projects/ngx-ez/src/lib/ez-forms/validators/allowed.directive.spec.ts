import { AllowedDirective } from './allowed.directive';

describe('AllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowedDirective();
    expect(directive).toBeTruthy();
  });
});
