export interface EzControlProperties {
  dirty: boolean;
  fieldset: boolean;
  invalid: boolean;
  labelledby: string;
  maxlength: string;
  message: string;
  name: string;
  placeholder: string;
  pristine: boolean;
  readonly: boolean;
  required: boolean;
  submitted: boolean;
  valid: boolean;
}
