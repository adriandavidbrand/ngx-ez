import { EzFormReadonlyDirective } from './ez-form-readonly.directive';

describe('EzFormReadonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new EzFormReadonlyDirective();
    expect(directive).toBeTruthy();
  });
});
