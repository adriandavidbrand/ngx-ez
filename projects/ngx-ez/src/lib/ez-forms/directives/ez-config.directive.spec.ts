import { EzConfigDirective } from './ez-config.directive';

describe('EzConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new EzConfigDirective();
    expect(directive).toBeTruthy();
  });
});
