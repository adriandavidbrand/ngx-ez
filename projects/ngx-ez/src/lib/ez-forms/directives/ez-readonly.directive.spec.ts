import { EzReadonlyDirective } from './ez-readonly.directive';

describe('EzReadonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new EzReadonlyDirective();
    expect(directive).toBeTruthy();
  });
});
